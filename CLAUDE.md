# CLAUDE.md — 澄家リフォーム Mini サンプルサイト

## プロジェクト概要
- Promeon Web の **Miniプラン制作サンプル**（1 ページ・静的 HTML / CSS / 最小 JS）。
- 想定クライアント「澄家リフォーム（すみかリフォーム）」は**架空**。一人で営む地域密着型リフォーム店。
- コンセプト:「工事だけでなく、来たときよりも気持ちよく。」= 清潔・丁寧・誠実・話しやすい。
- ターゲット: 札幌市内の 40〜70 代の戸建て所有者。小規模修繕〜部分リフォーム需要。
- コンテンツの事実（屋号・所在地・電話・施工事例など）は案件ブリーフを唯一の出典とする。勝手に創作・増減しない。

## デザインの基準
- **デザインシステムは [DESIGN.md](./DESIGN.md) に定義済み。**
- **デザインに関わる変更（色・タイポ・余白・レイアウト・radius・shadow・アニメーション）を行う前に、必ず DESIGN.md を読むこと。**

## ルール（厳守）
1. **デザイン変更前に必ず `DESIGN.md` を確認する。**
2. **`DESIGN.md` に存在しない色・radius・shadow・font-size・font 種類・アニメーションをむやみに追加しない。**
   必要な場合は、実装より先に `DESIGN.md` を更新し、矛盾がないか確認してから反映する。
3. **既存のデザインシステム（トークン・スケール）を優先する。** ページ単位での独自値の追加は禁止。
4. **依頼されていないコンテンツを勝手に削除しない。** セクション・文章・画像・リンクの削除は明示指示があるときのみ。
5. **既存機能を壊さない。** フォーム送信、アンカーリンク、ナビ開閉、`tel:` リンク等の動作を変更する場合は影響範囲を先に確認する。
6. **大きな変更の前に現在の構造を確認する。** 対象ファイルと関連 CSS/JS をまず読み、既存のクラス命名・記法に合わせる。
7. **PC / Tablet / Mobile の 3 表示を常に考慮する。** ブレークポイントと各余白・文字サイズ・レイアウト変更は DESIGN.md §10 に従う。
8. **日本語の改行を確認する。** 見出し・本文で不自然な折り返しや 1〜2 文字の孤立が出ていないか確認する（DESIGN.md §11）。
9. **AI 生成テンプレっぽさを避ける**（DESIGN.md §12）。装飾より写真・タイポ・余白・グリッド・罫線・背景切り替え・レイアウトの強弱を優先する。

## 作業スコープ
- この案件のファイルは `sanpuru-mini/` 配下に閉じる。リポジトリ親（Promeon Web 本体サイト: `../index.html` ほか）は変更しない。
- 工程は「①分析 →②デザインシステム →③制作」の順。各工程で指示された範囲を超えて先に進まない。
